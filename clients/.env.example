# BugKillers QA Agent — Credenciais do Cliente
# Copie este arquivo para clients/<id>/.env e preencha com os valores reais
# NUNCA commite o arquivo .env no repositório

# Senha do usuário de QA (deve corresponder ao envPassword definido em config.json)
QA_PASSWORD=
